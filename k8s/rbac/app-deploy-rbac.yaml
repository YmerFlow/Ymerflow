---
# ServiceAccount the in-cluster yf-deploy-app Job runs as (see prod/runall-production.sh and
# docs/plans/app-deployment-hooks.md Phase 5). Distinct from the ymerflow namespace's `default`
# SA (which carries the backend's own job-running identity, k8s/rbac/backend-jobs-rbac.yaml) so
# the broad app-deployment permissions below aren't granted to every backend pod — only to the
# short-lived deploy Job.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ymerflow-app-deployer
  namespace: ymerflow
---
# Namespaced Role: everything backend.services.app_deployment.apply_app_workloads() +
# NodePortAppDeploymentMixin.expose_app() do is namespaced to the app namespace — manage the
# backend/frontend Deployments, their Service + the frontend NodePort Service, the
# ymerflow-backend-secret + app pull Secret, and the one-shot DB migration Job (create,
# then read its pod logs). No cluster-scoped permissions are needed.
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: ymerflow-app-deployer
  namespace: ymerflow
rules:
- apiGroups: ["apps"]
  resources: ["deployments"]
  verbs: ["create", "get", "list", "watch", "update", "patch"]
- apiGroups: [""]
  resources: ["services", "secrets"]
  verbs: ["create", "get", "list", "watch", "update", "patch"]
- apiGroups: ["batch"]
  resources: ["jobs"]
  verbs: ["create", "get", "list", "watch", "delete"]
- apiGroups: [""]
  resources: ["pods", "pods/log"]
  verbs: ["get", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: ymerflow-app-deployer
  namespace: ymerflow
subjects:
- kind: ServiceAccount
  name: ymerflow-app-deployer
  namespace: ymerflow
roleRef:
  kind: Role
  name: ymerflow-app-deployer
  apiGroup: rbac.authorization.k8s.io
